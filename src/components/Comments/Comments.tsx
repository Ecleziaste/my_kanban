import React from "react";
import "./styles.css";

type Props = {};

const Comments: React.FC<Props> = () => {
  // const commentsByCardId = comments.filter((comment) => comments.id === id);

  return <div className="comments__wrapper">Comments
  </div>;
};

export default Comments;
